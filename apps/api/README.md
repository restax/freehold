# apps/api

Node/TypeScript backend, deployed on Railway.

Not yet scaffolded — built out starting in [Stage 00](../../docs/PLAN.md#stage-00-foundations). Owns auth, the Postgres/Prisma data layer, task/workflow engine, and all calls out to `packages/gateway-client`.
