# Master to-do list

The single list of everything open across stages. Items move to ~~struck~~ with a date when done.

## Launch blockers (Stage 06 exit: repo public, demo live, first outside signups)

- [x] **CLA final legal text** in [CLA.md](CLA.md) (Apache ICLA V2.2 adaptation with dual-license grant); signing gate workflow already in place *(2026-07-18)*
- [x] **Self-hosting guide** at [SELF-HOSTING.md](SELF-HOSTING.md); site buttons and footer now link to it *(2026-07-18)*
- [x] Self-healing demo *(2026-07-19)* — /api/demo checks the workspace skeleton (transactions + clients) before sign-in; if a visitor gutted it, the whole demo silently wipes and re-seeds on the spot (one count query on the happy path, failures never block entry). Verified: deleted everything → next entry restored 2 txns / 1 client / 14 email templates. Nightly cron remains the backstop
- [x] **Seeded demo instance** *(2026-07-19)* — "Explore the live demo" on the landing page → `/api/demo` signs visitors into the shared "Maplewood Transactions (Demo)" workspace as a plain member (owner/admin actions locked); Vercel cron wipes and re-seeds nightly at 9:00 UTC via `/api/demo/reset` (CRON_SECRET-guarded); demo only activates when `DEMO_USER_PASSWORD` is set, so self-hosters never expose one
- [ ] **Launch posts** — drafts ready for Paul's review in [launch/](launch/): Show HN, Product Hunt, r/selfhosted *(drafted 2026-07-19; post only after repo flips public + honesty items resolved)*
- [x] **Pushed to github.com/restax/freehold** (private; history triple-scrubbed: secrets, personal names, banned words; CI green). Flip to public when ready: `gh repo edit restax/freehold --visibility public` *(2026-07-18)*
- [x] **Vercel deploy** of freeholdtc.dev *(2026-07-18)* — LIVE with TLS: project `freehold` (root `apps/web`, turbo build), Hostinger nameservers → Vercel DNS with all email records (MX/SPF/DKIM/DMARC/autodiscover) pre-copied so hello@/partners@ kept working, www 308→apex. Neon Postgres via Marketplace: build step `scripts/vercel-db-setup.mjs` creates non-owner `freehold_app` role + runs `prisma migrate deploy` + grants each deploy; runtime derives its connection from `STORAGE_DATABASE_URL` with `freehold_app` credentials (RLS intact). Stripe test-mode webhook endpoint live at /api/webhooks/stripe (subscriptions + invoices). Verified end-to-end on prod: signup → workspace create → dashboard, /api/health db:true. Leftovers: test accounts to delete eventually (smoketest@freeholdtc.dev / "Smoke Test TC", onboarding-test@freeholdtc.dev / "Onboarding Test Co"); `REDIS_URL` still placeholder (nothing uses it yet); apps/api (Fastify) not on Vercel — host separately when public API goes live.
- [x] **hello@freeholdtc.dev mailbox** set up; partners@ already existed *(2026-07-18)*
- [x] Google Search Console verified + sitemap submitted *(2026-07-19)* — DNS TXT google-site-verification on freeholdtc.dev (Vercel DNS rec_21798d837dd1d9aac29afdf7); /sitemap.xml (14 marketing/docs URLs) + /robots.txt (app routes disallowed) live and submitted. Optional later: Bing Webmaster Tools can import the verified GSC property in one click (no approval process)
- [x] OG/social share image *(2026-07-19)* — Higgsfield Soul V2 photo (Flux Kontext pass removed AI-gibberish lettering) + real Outfit type overlay, rendered at 2x via headless Chrome; installed as `apps/web/src/app/opengraph-image.png` + `twitter-image.png` (Next file convention, alt text included), verified live in meta tags

- [ ] **(Paul — Stripe not finished) Payments PAUSED site-wide** *(2026-07-19, Paul's overnight order)* — `PAYMENTS_PAUSED = true` in `apps/web/src/lib/payments-paused.ts`: pricing-page Pro/Business CTAs open a "Start free today" dialog (no explanation shown, per Paul) instead of signup, the billing page hides its upgrade buttons, and `startUpgrade` refuses to create Stripe checkout sessions even on a direct POST. **To re-enable once Stripe is fixed: flip the constant to `false` and redeploy** — everything returns exactly as it was
- [ ] **(Paul decision) Outbox cron is daily, not hourly** — Vercel Hobby rejects sub-daily crons (`0 * * * *` failed the deploy), so `/api/outbox/run` now fires once at 13:00 UTC (= 8am Chicago in summer, right when default quiet hours end — deferred emails still land on time). "Send later" emails at other times wait for that daily run. Options for true hourly delivery: (a) Vercel Pro ($20/mo), or (b) free GitHub Actions hourly workflow curling the endpoint with CRON_SECRET — needs a commit+push, ready to build on request

## Marketing honesty items (site currently promises these)

- [x] Free-tier "AI extraction trial credits" — implemented *(2026-07-19)*: 10 lifetime trial extractions on Cloud Free (durable `ai_extractions_used` counter on organization; consumed only on success; enforced in the action, surfaced on the Extract button and billing page; paid tiers fair-use unmetered; demo reset refreshes credits; pricing page says "10 AI contract extractions to try it")
- [x] Partner account / fleet dashboard / consolidated invoicing — pricing page reworded to honest "works today vs in development" framing with partners@ CTA *(2026-07-19)*
- [x] Business tier definitions *(2026-07-19)* — "White-glove onboarding" → "Onboarding done with you: send your exports, we set up your workspace on a call"; "first access to reporting and invoicing" → "Early access to new features, reporting first" (invoicing is live, now listed on Pro)

## Growth & operations (added 2026-07-19)

- [x] **Operator admin panel** at `/admin` *(2026-07-19)* — workspaces table (plan, seats, active txns vs cap, AI credits used, Stripe customer link), topline stats (tenants, paying, MRR, users, 7-day signups), recent signups; read-only; gated by `PLATFORM_ADMIN_EMAILS` env (set to Paul's email on Vercel; unset on self-host = no panel)
- [x] **Site chatbot messenger** *(2026-07-19)* — Claude-powered widget on all marketing pages (`/api/chat`, honest system prompt with real pricing/features, points to hello@ when unsure); every conversation forwards to Slack when the webhook is configured
- [x] **Slack operator alerts** *(2026-07-19, scaffolded)* — new signups, plan changes, chat messages post to `SLACK_ADMIN_WEBHOOK_URL`. **(Paul)** create the webhook: api.slack.com/apps → Create App → Incoming Webhooks → pick channel → copy URL → add as `SLACK_ADMIN_WEBHOOK_URL` in Vercel env (no approval process)
- [x] **Website tracking** *(2026-07-19, scaffolded)* — Vercel Web Analytics component mounted (**Paul**: enable the toggle at vercel.com → freehold project → Analytics tab); PostHog wired for robust events/funnels (**Paul**: free account at posthog.com → project API key → add `NEXT_PUBLIC_POSTHOG_KEY` in Vercel env, then redeploy)
- [x] **Transactional email + reply capture** *(2026-07-19, code complete — awaiting Paul's Resend signup)* — no IMAP/SMTP/OAuth anywhere: outbound via Resend from `<workspace-slug>@EMAIL_FROM_DOMAIN` with per-thread `reply+<token>@EMAIL_REPLY_DOMAIN` Reply-To; inbound webhook (svix signature-verified) threads replies back onto the transaction; Emails tab = compose (party-email autocomplete) + thread view with delivery-status badges; Email table (RLS) + email_thread capability table (no RLS, portal_link precedent); audit + demo-reset wired. Inbound pipeline live-verified locally with signed payloads. **(Paul)** create resend.com account → `RESEND_API_KEY` into .env; then: automate domain setup (mail. + reply. subdomains via Resend API + Vercel DNS), create the webhook (real secret), live send/reply test, then marketing
- [ ] **Tenant messaging notifications** — per-workspace Slack/Discord/Telegram webhooks for transaction/task events (all approval-free; WhatsApp + FB Messenger intentionally skipped — Meta review required, same policy as Google)

## Product backlog (pre-launch polish)

- [ ] **(Paul) Documenso review** — evaluate Documenso as the e-sign story before launch: sign up / spin up an instance, connect it via Integrations → Documenso (paste URL + API token), and run a real envelope end-to-end from a transaction's Attachments tab (send → sign → status back). Decide: bundled shared instance for Cloud (Paul hosts, ~$10-20/mo VPS, per-tenant tokens auto-provisioned — the plumbing already exists) vs per-tenant BYO only. Marketing currently promises "bundled open-source e-signing" — this review confirms or adjusts that claim

- [x] DocuSign adapter live test *(2026-07-19)* — JWT grant + 3 sandbox envelopes + status polling all verified (21 eSignature calls, clearing go-live's ~20-call bar). Fixed en route: adapter now accepts PKCS1 keys (what DocuSign's console generates), not just PKCS8. Key rotated after transcript exposure and re-verified live. **Go-live blocked on plan gating**: trial/standard accounts can't host a Public integration key → Paul submitted the free ISV Partner Program application 2026-07-19 (pending). **Marketing decision (Paul)**: all DocuSign + Dotloop references scrubbed from the site and launch drafts — the e-sign story is open-source Documenso, bundled; the DocuSign adapter stays in code (self-hosters configure their own credentials). If partner approval lands, re-add marketing + build the per-tenant Connect flow (client secret saved)
- [ ] Extraction prompt: request 2-letter state codes
- [ ] Duplicate party roles (two sellers) collide on one custom-field key — needs suffixing
- [ ] Member-role button hiding in UI (server-side gating exists; buttons still render)
- [x] Client detail pages *(2026-07-19)* — clients are clickable: transactions list + portal access panel (activate/deactivate any sign-in; same link resumes on reactivate); delete moved off the list page into an admin-only, type-DELETE-to-confirm danger zone
- [x] Delete safeguards everywhere *(2026-07-19)* — every destructive delete (client, transaction, contact, task, document, envelope, action plan, template, portal link, webhook endpoint) now requires typing DELETE, enforced server-side (`confirmed()` in lib/forms) with the DangerDelete component as the UI
- [x] Marketing & docs catch-up *(2026-07-19)* — features page: new "Client & agent portals" group, dual-person CRM + Claude-skill entries in "Only in Freehold", CRM statuses refreshed (grades/ownership now "today"), and a **"Real screenshots, not mockups"** tour of 4 production captures from the demo workspace (public/marketing/screens/, taken via headless Chrome against a prod-mode server; `/api/demo?next=` param added for authenticated captures); integrations page: Calendar feeds + Freehold API moved to "Working today", Claude entry mentions the skill; dashboard What's-new gains skill + portals entries. Screenshots to refresh whenever the UI shifts meaningfully
- [x] Claude skill for TCs *(2026-07-19)* — `skills/freehold/SKILL.md` in the repo: Claude queries the TC's workspace via the API (account overview, clients + portal activity, transactions, deadlines, contacts) with their `fh_live_` key. To make it work on Cloud, **API v1 is now mounted in the web app** at `/api/v1/*` (account + clients endpoints added to both the web mount and the Fastify service; docs page updated). TODO: unify the two implementations; publish the skill somewhere installable once the repo is public
- [x] Portal video-spec round *(2026-07-19)* — client notes on the backend client page (RLS'd client_note table, "internal only" framing); **ICS calendar feeds** per portal link (`/portal/<token>/calendar.ics`, both audiences — subscribe from Google/Outlook/Apple, date changes sync on refresh; X-WR-CALNAME'd); agent portal "Next 30 days" upcoming-dates widget; "View as agent" + prefilled "Email link" actions on the client page. Remaining from the video spec (tracked): white-label logo upload + independent-TC branding toggle, per-date visibility toggles, template/global visibility defaults, timezone setting, once-per-device token verification (ours is pure capability links — simpler, arguably fine)
- [x] Transaction page buildout, round 1 *(2026-07-19, Paul's spec)* — list table gains Buyer/Seller (party names), DOM (from on-market/list date), MLS ID, list-vs-contract price; create form adds list price/dates, MLS ID, and assignment roles (co-agent = managed agent/Client, TC 1 & 2 = workspace users); detail page is now the 3-column layout: left = collapsible Listing details + custom fields (with `{{merge_code}}` hint), center = 8 tabs (Tasks / Attachments / Dates & details / Participants / Emails / Notes / Payout / Portals & misc), right = key dates + next deadlines quick view; Payout tab computes gross from commission % against contract price
- [x] Contract-governed dates + cascading dates + task priority *(2026-07-19, Paul's design: "the contract is the ultimate source of truth")* — governed dates (contract/close) are never changed silently: edits become **proposals** with an auto-created HIGH-priority "Amendment needed" to-do due immediately; "Amendment executed — apply" applies the date, **recomputes every open anchored task** (plan tasks now persist anchor+offset; manually re-dated tasks opt out), completes the amendment task, and writes the audit trail; Withdraw skips it. Tasks have **priority** (Normal/High/Critical, flag-cycle on rows, badges); the day dashboard sorts priority-first, and amendment tasks **auto-escalate to Critical within 2 days** of the governed date. Live-verified end-to-end: propose → date unchanged → confirm → close +7d moved 11 anchored deadlines correctly; direct form edit also routes to proposal
- [x] Client → agents → per-agent portal access + credential assignment *(2026-07-19, Paul's spec)* — new `client_agent` join (RLS'd): attach any contact to a client as an agent from the client page; **portal access is granted per agent** ("Give portal access" creates an AGENT portal link tied to that contact — agents who don't want one never get a link; detaching deactivates, never deletes); vault credentials now assignable to a **contact** as well as a client — client page shows all credentials for the client and its agents, contact page gains a Credentials tab (list + audited reveal + inline add), vault gains an agent select + "Belongs to" column; contacts gain **profile photo URL + social links** (Facebook/Instagram/LinkedIn/X/YouTube/other) shown in the header and editable in the form. Legacy client-wide portal links still render under the Agents card
- [x] **Relicensed Apache-2.0 → FSL-1.1-Apache-2.0** *(2026-07-19, Paul's decision — protect Cloud from competing hosts, done pre-public-flip so it's a non-event)* — root LICENSE is now Sentry's Functional Source License verbatim (notice: "Copyright 2026 Freehold (freeholdtc.dev)"): free to use/modify/self-host for your own business, competing hosted service prohibited, each release auto-converts to Apache-2.0 after two years. All package.json license fields, README, CONTRIBUTING, CLA (dual-license grant now names FSL + future grant), ee/LICENSE + ee/README cross-references, PLAN.md updated. **Marketing scrubbed "open source" → "fair source"** everywhere it described Freehold (landing FAQ + hero + exit-clause card, features, compare, terms, footer, nav "Open source"→"Self-host", layout meta/OG alt text, chatbot prompt, all three launch drafts — r/selfhosted draft says plainly "fair source, not OSI open source"); Documenso/Twenty keep "open-source" (true of them). **(Paul)** two follow-ups: file the trademark (USPTO, ~$250-350/class), and optionally replace "Freehold (freeholdtc.dev)" in the LICENSE notice with your legal entity name once you have one — a lawyer hour before launch is cheap insurance
- [ ] **(Paul — LAUNCH BLOCKER) Offline secret custody** — before any real customer: store durable offline copies (password manager + a printed copy in a safe) of `VAULT_MASTER_KEY` (losing it = every credential AND every encrypted document permanently unrecoverable, by design — no backup helps), `BACKUP_PASSPHRASE` (decrypts the nightly dumps; now in .env), and the rest of .env (auth secret, Stripe, Resend). docs/BACKUP.md documents the rule
- [x] Document encryption at the application layer *(2026-07-19, Paul's decision)* — every uploaded document is envelope-encrypted (AES-256-GCM, per-object data key wrapped by VAULT_MASTER_KEY — same scheme as the vault) before touching the DB or S3: new encryptBytes/decryptBytes/isEncryptedBytes in @freehold/vault (self-describing FHE1-prefixed payload), wired at the single putObject/getObjectBytes choke point (all consumers — downloads, portals, ZIP, e-sign, AI extraction — route through it; verified); backward compatible (payloads without the magic prefix pass through, so pre-encryption docs never break); only active when VAULT_MASTER_KEY is set (keyless self-hosts keep working). **Live-verified**: intake upload stored as FHE1 ciphertext in the DB, downloaded back as %PDF byte-identical
- [x] Nightly offsite backup *(2026-07-19)* — .github/workflows/backup.yml: daily 07:17 UTC pg_dump (owner URL) → gzip → AES-256-CBC (pbkdf2) encrypted → GitHub Actions artifact on the private repo, 30-day retention + manual dispatch; repo secrets BACKUP_DATABASE_URL + BACKUP_PASSPHRASE set via gh (passphrase also in .env); **docs/BACKUP.md** = full restore runbook (Neon PITR, artifact decrypt, psql restore, role re-creation via vercel-db-setup.mjs, env swap, 4-step smoke test) + the master-key rule. **Starts running after the next commit+push**; then trigger one manual run to prove the pipeline
- [x] Security marketing *(2026-07-19)* — features Security group gains "Documents encrypted at rest" + "Offsite encrypted backups"; homepage isolation card now says documents+credentials envelope-encrypted at the application layer ("even direct database access yields ciphertext"); terms disclaimer + privacy updated to match reality
- [x] Security disclaimer + user-responsibility terms *(2026-07-19, Paul's spec)* — /terms gains two sections: **"Security disclaimer"** (industry-standard safeguards named honestly — encryption in transit/at rest, RLS isolation, envelope-encrypted vault, audit logging — but "no system connected to the internet is 100% secure, and **we can make no guarantees as to the security or privacy of your information**; you use the service at your own risk") and **"Your responsibilities"** (solely responsible for data + access: passwords/API keys, portal-link sharing, client consent before storing info/credentials, deactivating stale access, legal compliance; self-hosters solely responsible for their servers); /privacy mirrors briefly and links the terms; **signup now shows consent**: "By creating an account you agree to the terms and privacy policy, including the security disclaimer" — which is what makes it stick. Both pages still carry the "finalized with counsel before paid customers" note — keep that lawyer hour on the pre-launch list
- [x] **Relicensed FSL → Elastic License 2.0** *(2026-07-19, Paul's decision: "nobody can fork or resell my open source as a service or another brand... not even a single copy hosted")* — FSL's 2-year Apache conversion was the hole (copycats could legally host the aged version); ELv2 has **no conversion, ever**, an explicit no-hosted-or-managed-service clause, a no-license-key-circumvention clause (legal hook if key gating is ever added), and trademark protection. LICENSE = official ELv2 verbatim + Freehold copyright line; all package.json → Elastic-2.0; README/CONTRIBUTING/CLA/ee refs/PLAN updated; full marketing sweep "fair source" → **"source-available"** with the plain promise "self-host free for your own organization, forever — never resellable or hostable for others" (landing FAQ/hero/meta/OG, features "The code is public", terms, compare — IT-provider column reworded to "on servers you control", footer, chatbot, all launch drafts incl. r/selfhosted honesty paragraph). Both prior licenses never shipped publicly, so this is still a day-one licensing choice
- [ ] **(Paul decision) License-key gating for self-host** — recommendation: **don't** gate the core. It contradicts "no license keys" (a core differentiator vs the platforms TCs hate), is trivially stripped from public source (the license forbids that — but then it's the license doing the work anyway, keys add no legal power), and kills the self-host adoption funnel that feeds Cloud. The real fence = ELv2's no-hosting clause + registered trademark + your shipping speed. Revisit only if real abuse appears; ELv2's anti-circumvention clause already makes future key-gating (e.g., of ee/ features) legally enforceable
- [x] Marketing checklist sweep *(2026-07-19, Paul's competitor-comparison list)* — features page now explicitly names: **Task automation** + **Email automation** (Workflows), **Brand customization** + **Works on any phone** (portals group), and a new **Security** group (RLS isolation, encrypted vault, full audit log, signed/revocable links + HMAC webhooks, type-DELETE safeguards, 2FA listed as "on request"). AI extraction, intake forms, training/support (pricing + /services) already covered. Gmail/Drive intentionally skipped (Paul's no-Google policy). Honesty notes: we say "encrypted at rest / envelope-encrypted vault", never "end-to-end encrypted" (server-side rendering means true E2EE would be false); analytics stays honest today/request
- [ ] **(Paul — before repo flip + soon regardless) Rotate the 2FA backup codes that touched GitHub** — a `.site2FA` file with backup codes was committed and pushed for ~2 minutes on 2026-07-19 before being amended out (force-push ffa4d02 removed it from the branch; commit e4959c3 may persist as an unreferenced object on GitHub until GC). The file is now gitignored and stays on disk only. Fix: Settings → Two-factor authentication → disable → re-enable = brand-new codes; move the saved codes OUT of the repo folder (password manager). This also joins the pre-flip history-scrub checklist
- [x] Email template studio — Stage 1 *(2026-07-19, Paul's redesign demand: "email templates are a core feature")* — **split pages**: /dashboard/templates = Doc templates (PDF merge letters) only; new **/dashboard/emails** = the email studio (nav: "Doc templates" + "Email templates"). Studio: **Signature & footer card** (workspace-wide, org.emailSettings, rendered into every outgoing email — change once, applies everywhere incl. automated); **TemplateEditor** client component used on every body field (formatting toolbar B/I/H1/H2/bullets inserting markdown-lite at cursor, **merge-field picker** dropdown, **Dictate** button, **live preview** of the rendered branded email — deliberately not an RTE); library **grouped by category** (Status/Intro/Portal/Contract-to-close/Listing/Post-close/Other) with create/edit/type-DELETE; **14-template starter library** in lib/email-template-library.ts (EMD w/ wire-fraud warning, inspection ×2, appraisal, clear-to-close, title/escrow, walkthrough+closing, listing live, showing feedback, post-close, review ask, status/intro/portal) with per-template **task-match keywords**; editing a sample clears its isSample flag; **task-aware suggestions**: emailing from a task splits chips into highlighted "Suggested for <task>" + "All templates" (verified: intro task → Introductions template suggested); refresh migration (delete isSample + reinsert library ×14) generated from the same TS source so existing workspaces got the new library. Features page copy updated.
- [x] Email template studio — **Stage 2** *(2026-07-19)* — **plan-task default templates**: ActionPlanTask.emailTemplateId (select in the plan editor + ✉ badge per row) carried onto instantiated Tasks; task rows with a template show a green ✉ that deep-links compose with that exact email merged and ready (verified: inspection task → "Inspection scheduled — 412 Maple" prefilled); **attachments**: compose gains an "Attach documents" checkbox list of the transaction's files (decrypted via getObjectBytes, base64 to Resend, 15MB cap), and templates carry attachMatch keywords that pre-check matching filenames (verified: attachMatch "inspection" pre-checked "Inspection report.pdf"); **usage counts**: EmailTemplate.usageCount incremented on templated sends, "used N×" chip in the studio
- [x] Email template studio — **Stage 3** *(2026-07-19)* — **task-completion auto-send**: ActionPlanTask/Task gain autoSendEmail ("Auto-send on completion" checkbox in the plan editor, "· auto" badge); completing a task with a template + auto flag renders the merge and emails the client automatically (skips .example demo domains, per-client prefs still apply). **Quiet hours**: org.emailSettings gains quietStart/quietEnd/timeZone (studio Signature & footer card, default 8pm–8am America/Chicago) — automated sends during the window are deferred, a 2am completion becomes an 8am email. **Outbox**: new email_outbox platform table (no RLS, documented) + lib/outbox.ts (enqueueOrSend, flushOutbox with ≤5 retries + lastError); hourly Vercel cron /api/outbox/run (CRON_SECRET bearer) delivers due rows, rendering branded HTML at send time so signatures/parties are current. **Scheduled sends**: compose gains "Send later" (datetime picker → outbox + audit event), a Scheduled section on the transaction listing pending emails with Cancel (cancelScheduledEmail). Two real bugs found in verification: floating promises are dropped when a server action responds — ALL fire-and-forget email work now wrapped in next/server after() (this was silently affecting prod lifecycle emails too); parseQuietHours originally read parseEmailSettings output which strips the quiet keys → always defaulted. Verified live: task completion during a quiet window → outbox row queued for window end (not sent). Features page: new "Scheduled sends and quiet hours" entry; per-client frequency guardrails deferred to backlog
- [x] Pricing v3 + trials + coupons + operator analytics + carts + email OTP *(2026-07-19, Paul's spec)* — **Pricing**: Pro $40 (2 users, 50 active txns, 50 clients with portals), Business $85 (10 users, 100 active txns, 100 portal clients), Free gains explicit 5-portal-client cap; new Stripe prices wired (.env + Vercel), plans.ts + new portalClientLimit() enforced in both portal-creation actions; pricing page rewritten (per-tier notes: "No credit card required" on Free, "7-day free trial · cancel in two clicks" on paid; "All billing runs through Stripe — we never see or store your card"); chatbot + landing + launch drafts updated. **Trials**: checkout subscription trial_period_days 7 + allow_promotion_codes + after_expiration recovery (all verified via live session create). **Coupons**: ee helpers (create $-off-per-month or 100%-free repeating coupons + typeable promotion codes — Stripe v22 promotion:{type:coupon} shape; apply to a subscription's future months via discounts:[{promotion_code}]); /admin gains Coupons section (create + list w/ redemption counts) and per-workspace "Apply" input on rows with active subs; WELCOME10 ($10/3mo) + FREEMONTH2 (free 2mo) created live as first real codes. **Analytics** on /admin: new subscriptions / cancellations / in-trial (Stripe, last 30d) + free-tier signups; visitors note points at Vercel Analytics/PostHog (Paul's pending toggles). **Abandoned carts**: checkout_attempt table records every session (billing action), webhook closes it on checkout.session.completed/expired (events added to the Stripe endpoint) and captures the 30-day recovery URL; /admin lists open/expired with mailto links for manual outreach. **Email verification (6-digit)**: better-auth emailOTP on Cloud (requireEmailVerification gated to FREEHOLD_CLOUD + Resend so self-hosts never lock out), platform sender no-reply@mail.freeholdtc.dev, /verify-email page (enter code, resend), signup routes there when no session, login 403 auto-sends a fresh code, verified banner on login; demo accounts auto-verified; existing 4 users backfilled verified. OTP flow verified end-to-end (no session until code entered; sign-in works after)
- [x] Email template library + task-linked emails + dictation + task-type coverage *(2026-07-19, Paul's spec, extra effort)* — **Email template library**: new RLS'd email_template table, full CRUD on /dashboard/templates (create/edit/type-DELETE), 4 seeded samples (Status update, Introductions — lender & title, Inspection scheduled, Portal invitation); merge codes grew to 14 (buyer/seller/agents/lender/title + task_title/task_due). **Task → email**: ✉ on every task row on the transaction opens the Emails tab; template chips prefill subject+body with merge fields rendered server-side (TC sees final text, fully editable) — verified live. **Formatting**: deliberately chose markdown-lite (**bold**, _italic_, # headings for size, - bullets) rendered into the branded HTML over an embedded rich-text editor — RTEs are unreliable across browsers and their HTML across email clients; hint shown under compose. **Deepgram voice dictation**: Dictate button on compose (MediaRecorder → /api/transcribe → Nova-2 smart_format), paid-Cloud-tiers via platform key / self-host BYO DEEPGRAM_API_KEY / Cloud Free → 402 with billing upgrade prompt — all three verified live (say-generated speech transcribed flawlessly incl. punctuation); key added to Vercel. **Task-type coverage**: 3 new prebuilt sample plans (Documents & compliance, Listing prep, Post-close follow-up) joining the contract-to-close plan — Paul's task-category list mapped: milestones/compliance/communication/listing/post-close all have prebuilt tasks or existing features (Today dashboard, assignees, portal visibility toggles, touch dates already live); tenants add their own via Action plans (existing). Demo re-seeded locally; prod demo re-seeds via nightly cron (manual reset 401'd — local CRON_SECRET differs from Vercel's; harmless). Features page: "Email templates attached to tasks" → today, new "Voice-to-email dictation" entry stressing Deepgram accuracy; SELF-HOSTING gains DEEPGRAM_API_KEY section
- [x] **MFA / two-factor auth** *(2026-07-19, Paul's go)* — better-auth twoFactor plugin (TOTP, issuer "Freehold") + twoFactorClient with /two-factor redirect; two_factor table + user.two_factor_enabled migration; **Settings → Two-factor authentication**: password → QR (rendered with the qrcode lib) + 10 one-time backup codes (shown once) → first code activates; disable requires password; **/two-factor** challenge page at sign-in (TOTP or backup code, trust-device 30 days). API-verified end-to-end: enable → activate → sign-in returns twoFactorRedirect with NO session → TOTP verify grants session; backup-code path verified too. Features page 2FA → "today"; SELF-HOSTING doc section added. Note for later: Clerk evaluated and rejected (full auth replacement, per-MAU cost, third party in the sensitive-data path); Google/Microsoft *sign-in* via better-auth socialProviders is already scaffolded in auth.ts — just add GOOGLE_/MICROSOFT_CLIENT_ID+SECRET env (basic sign-in scopes need no Google review, unlike the banned Gmail/Drive scopes)
- [ ] **(Paul + build) Business analytics** — only live pipeline counts exist today; competitor lists tout "business analytics". Backlog already holds agent-portal analytics charts; a reports tab (closings/month, volume, task throughput, per-client activity) would complete the story. Features page keeps it honest as "request" until built
- [ ] **(Paul decision) Mobile platform** — today = responsive web (marketed honestly as "Works on any phone"). Options when ready: PWA manifest + install prompt (cheap, no store approvals) vs native wrapper (App Store review = approval process, against current policy). PWA recommended
- [x] Pricing raise *(2026-07-19, Paul's decision)* — Pro **$29 → $35/month** (2 users, **100 active transactions** at a time, was 50); Free tier **10 → 5 active transactions**; "active at a time" wording kept (the mechanic counts in-flight files — closed files free the slot; kinder + already built vs a monthly meter); **"client & agent portal logins are unlimited and never count as users"** added to Free + Pro feature lists. New Stripe price price_1TurhoLTzUACnU9jjbj2x4JZ on the Pro product (usd pinned), STRIPE_PRICE_PRO swapped in .env + Vercel; existing subscribers grandfather automatically (Stripe never moves them). Updated: pricing page, plans.ts PLAN_INFO, landing FAQ + CTA line, chatbot prompt, all three launch drafts, README. Business unchanged ($80/10 users/200)
- [x] Branded HTML emails + automated lifecycle emails + editable templates *(2026-07-19, Paul's spec — "email looked very plain")* — **renderEmailHtml** in lib/email-template.ts: table-based, image-free, email-client-safe (green header wordmark, clean paragraphs, contact cards: TC / client's agent+brokerage side-by-side, smaller other-side card auto-picked from parties by transaction side, "Powered by Freehold" footer with freeholdtc.dev link, UTF-8 meta); the compose path (Emails tab) now sends this HTML with text fallback. **Two automated emails**: intro (fires on transaction create) + post-close (fires on status → CLOSED transition), fire-and-forget, gated by per-client toggles on the client profile ("Automated emails" card: Intro / Post-close checkboxes, Client.emailPrefs), skipping .example/.test/.invalid domains (demo data can't bounce), templates per tenant in org.emailTemplates. **Editable wording** on /dashboard/templates ("Automated email templates" section: subject+body ×2, merge codes {{client_name}} {{property_address}} {{close_date}} {{contract_date}} {{tc_name}} {{tenant_name}}). **Seed** now creates "Closing intro letter (Sample)" doc template so /dashboard/templates isn't empty. Visual-verified via rendered preview. **Future release (Paul flagged as maybe-hard)**: tenant-defined custom emails with custom triggers (arbitrary events → arbitrary templates) — the emailTemplates/prefs plumbing is the foundation
- [x] "Age cheese, not your software." landing section *(2026-07-19, Paul's concept, 5-word headline)* — Higgsfield GPT Image 2 photograph of a Parmesan maturing room (cathedral light, dark lower third for text; optimized 510KB JPEG at public/marketing/parmesan.jpg); full-bleed band on the landing page between "Why TCs switch" and the Cloud sell, bottom-left anchor, italic same-family emphasis on "not", support line "Most TC platforms were built a generation ago. Freehold ships every week — and you can read the code.", single underlined CTA → /features. Also caught + fixed a leftover "Freehold is open source" claim in the "Never locked in" card (missed in the FSL sweep) → "free to self-host"
- [x] Portal intake forms + tenant side wording *(2026-07-19, Paul's spec)* — **intake forms with document upload on client portals**: buy-side form (legal names/title holding/lender/attorney…) and sell-side form (deed names/payoff/HOA/forwarding address…), fixed field sets in lib/intake.ts, DUAL transactions show both; uploads (≤5 files ×10MB) become client-visible "Intake —" documents on the transaction; submission stored in RLS'd intake_submission table + **HIGH "Review intake form" task due today**; thanks state on portal; token-capability auth (no session), per-link "Intake forms" toggle (show_intake, default on) on portal creation; submissions render on the transaction's Portals & misc tab. **Side relabeling**: org.sideLabels {buy, sell} editable in Settings → "Side wording" ("sell side / sale side / list side"), applied via lib/side-labels.ts across transactions list (filters + rows), transaction detail (header/selects/payout %), both portals, and intake form headings; SIDE_LABEL in format.ts remains the default fallback. Features page entry added. Live-verified: portal form → submission + doc + task, relabel → propagates portal + dashboard
- [x] Twenty CRM integration *(2026-07-19, Paul's ask — Bearer API-key auth, zero approvals)* — per-tenant connection (instance URL + API key from Twenty's Settings → API & Webhooks; works for api.twenty.com cloud or self-hosted; verified live before saving, encrypted, audit-logged, Disconnect clears): **leads out** — website registrations POST /rest/people ({name:{firstName,lastName}, emails:{primaryEmail}, phones:{primaryPhoneNumber}}, fire-and-forget alongside the FUB push); **people in** — "Import contacts now" pulls /rest/people (cursor-paginated ×60, 1000 cap, defensive parsing since Twenty workspaces have custom schemas) into contacts tagged "Twenty CRM", email-matched idempotent; org.twentyConfig migration. Docs: **/docs/twenty** 4-step setup; marketing integrations page moved Twenty from "on the way" to "working today". Negative path verified (api.twenty.com 401s bogus keys); positive path needs a real Twenty workspace — free tier exists if Paul wants to confirm end-to-end
- [x] Follow Up Boss integration *(2026-07-19, Paul's ask — API-key Basic auth, zero approvals)* — per-tenant connection on the Integrations page (key verified live against FUB before saving; encrypted with VAULT_MASTER_KEY; audit-logged; Disconnect clears): **leads out** — website registrations POST to FUB's /v1/events (per FUB docs, so tenant automations/action plans fire; fire-and-forget, never blocks the lead) with X-System: Freehold header; **people in** — "Import contacts now" pulls /v1/people (paginated, 1000 cap) into contacts tagged "Follow Up Boss", email-matched idempotent (no email → skipped; existing contacts never touched), last-import stats shown on the card; org.fubConfig Json migration. Docs: **/docs/followupboss** 4-step setup + honest scope note pointing at Zapier for deeper sync; marketing integrations page moved FUB from "on the way" to "working today". Negative path verified (FUB 401s bogus keys → nothing saves); positive path needs a real FUB account — **(Paul)** if you have one, paste its API key into the card once to confirm end-to-end
- [x] Zapier integration (DocuSign + Dotloop bridge, zero approvals) *(2026-07-19, Paul's decision)* — webhook catalog grew to 5 events (**document.uploaded, envelope.sent, envelope.completed** join transaction.created/task.completed; settings checkboxes render from the shared WEBHOOK_EVENTS list; emits wired in documents/esign actions incl. manual mark-signed); API v1 gains **POST /tasks, POST /notes** (timestamped append to transaction notes), and **GET/POST/DELETE /webhooks** for Zapier REST-hook auto-subscribe (secret returned once) — all live-verified with a temp key; **private Zapier app** scaffolded in integrations/zapier (zapier-platform-core 19, custom auth = URL + API key with /account test, 5 triggers incl. instant hooks, 4 actions; `zapier-platform validate` clean) — awaiting Paul's `zapier-platform login` to register + push + mint the invite link; **/docs/zapier** page with the DocuSign signing-loop and Dotloop sync recipes; in-app Integrations card + marketing integrations page moved Zapier to "working today". Zapier CLI v19 installed (binary is `zapier-platform`, needs Node 22)
- [x] Demo site "About us" + team photo *(2026-07-19, Paul's spec)* — Higgsfield photo of the two-woman Harborline team (small office, professional attire, on-palette) replaces the hero house photo on /example-site only; new short About section (Dana Whitfield & Priya Rao, ten years in, 40+ transactions/year, "small on purpose"). TenantSiteView gained optional heroImageSrc + about props — real tenant sites unchanged
- [x] Tenant website v2: white theme + logo + graphics + persistent example *(2026-07-19, Paul's spec, taste-skill + Higgsfield)* — published site redesigned white (split hero, bold display type, brand-green single accent, labeled form fields, services checklist); the awkward "Already a client?" portal blurb removed entirely; **two Higgsfield GPT Image 2 photographs** (green-door porch hero + brass-key closing-document detail, optimized JPEGs in public/site/) shared across all tenant sites; **logo upload** on /dashboard/website (PNG/JPEG/WebP ≤1MB → data URL in org.logo, shown on site header + hero, removable) — verified end-to-end via real upload; **freeholdtc.dev/example-site** = persistent fictional "Harborline Transaction Co." demo backed by no workspace (form stores nothing, banner links to features); features page mentions logo + example URL. Unpublished portal-entry page kept (copy tightened)
- [x] Tenant websites + QR codes *(2026-07-19, Paul's spec — "other platforms do not offer it")* — the subdomain (<slug>.freeholdtc.dev) is now a **publishable promotional mini-site**: tagline, about, contact info, service bullets, and a "Work with us" registration form that creates a "Website Lead" contact (buyer/seller lead type, message in notes) + same-day HIGH-priority follow-up task; honeypot spam guard; unpublished (default) keeps the plain portal-entry page, noindex. New **/dashboard/website** page (Business → Website, Globe icon): edit + publish/registration toggles (admin-gated), View site link, and **QR codes** (print-quality PNG + scalable SVG downloads, generated server-side with the qrcode lib) for cards/yard signs/flyers. Marketing features page: "Your own website on your subdomain" + "QR codes for your marketing" (both "today"). Verified live: publish → site renders → lead submits → contact + task land in workspace; subdomain host routing confirmed
- [x] Claude Skill one-click setup + IT services page *(2026-07-19, Paul's spec)* — Integrations "Claude Skill" card is self-contained: one click generates the key (admin-gated, stored hashed, shown once via short-lived cookie) and renders a **paste-ready Claude prompt** with the key and API base URL inlined + a copy button; "How does this work? Is my data safe?" plain-English disclosure (reads-mostly key, nothing deletable, HTTPS, revoke in Settings); non-admins told to ask an admin. DocuSign card reframed as "Self-hosted option" selling setup help → new marketing **/services** page (self-host install, DocuSign connection, data migration, email/domain, training; flat-fee framing; hello@ CTA) linked from the footer
- [x] Per-tenant Documenso connections *(2026-07-19, Paul's catch: Cloud tenants can't set env vars)* — Integrations page Documenso card is now tenant-actionable: paste instance URL + API token, **verified against the instance before saving** (bad credentials never stick, audit-logged either way), token encrypted with VAULT_MASTER_KEY on the organization row; Disconnect clears it. Adapter override plumbing (`makeDocumensoAdapter` + `getEsignAdapter(id, overrides)`) with env fallback for self-host defaults; send + status-poll both use the tenant connection. DocuSign card hidden on Cloud when unconfigured (env-only copy was self-hoster leakage). **DocuSeal evaluated and skipped for bundling**: API is Pro-gated even self-hosted ($20/user/mo license + ~$0.20/API completion + $20/tenant-admin/mo) vs Documenso's free self-host API. Next step for "bundled" signing: Freehold-hosted shared Documenso instance auto-provisioning per-tenant tokens into this same config
- [x] In-app Integrations page *(2026-07-19, Paul's ask)* — `/dashboard/integrations` (sidebar → Business): live per-workspace status cards for Email & reply capture (shows the workspace's send address when active), each e-sign adapter (Documenso/DocuSign, driven by adapter availability), Claude skill + Freehold API (key counts), signed webhooks, Stripe client invoicing, plus always-included Calendar feeds and CSV import; links out to the marketing roadmap for what's coming
- [ ] Transaction page round 2 remainder: custom-field collections/groups + highlight colors, task snooze/sub-notes + folders, attachment placeholders/folders/multi-select email, business-day/holiday date math in the formula sense, ICS invites per date, customizable statuses/roles, address auto-lookup, global search + notifications top bar, agent-portal analytics charts. Emails tab stays honest until IMAP/SMTP ships
- [x] Dual-person CRM rework *(2026-07-19, Paul's spec)* — one contact record holds two people ("Jordan & Casey Rivera"): parallel primary/secondary person fields, extra phones/emails, multi-select categories (+ create-your-own), A–D relationship grades driving an auto-prospecting cadence (A=30d B=60 C=90 D=180) with a durable next-touch date, "Prospecting due" dashboard card + due filter, home/work mailing addresses, owner assignment + admin setting restricting members to owned contacts, referral tracking (contact or source + date), buyer/seller lead tracking with per-type detail fields, yearly touch dates (birthday ×2, wedding & purchase anniversaries, month/day + optional year), post-save detail screen (quick note, follow-up scheduler creating contact-linked tasks, "Touched today" reset, tabs: Recent/Transactions/Notes/Tasks/Touch dates/Addresses/Other details), "+ Create" menu in the sidebar. Deferred honestly: Emails tab (needs IMAP connections), predictive search is a plain dropdown, CSV import mapping for the dual-person fields (importer still maps legacy name/email/phone)
- [ ] CSV importer: map dual-person columns (spouse first/last, categories, grades, addresses) into the new contact model
- [x] Two-portal system *(2026-07-19, Paul's spec)* — **Managed Agent Portal**: per-client link (created from the client page) with pipeline widget, on-track closings projection, 7-day activity feed (real audit/task data), searchable closed archive, per-transaction views with Download-all-ZIP; **Buyer & Seller Portal**: per-transaction, simplified — milestone timeline (coming up / completed), participant directory that auto-excludes the other side's agent + Google Maps link on the title company, document vault; **per-item visibility**: person-in-square (agent) + person-in-house (buyer/seller) toggles beside every task and document, enforced server-side incl. downloads and ZIP. Deferred honestly: showings-service feed (empty state pointing at the integrations roadmap), image logo upload (existing backlog item), portal user profiles (links are token capabilities, not accounts)
- [x] Audit trail (foundation) *(2026-07-19)* — audit_log table (RLS'd) + logAudit() helper; deletions and portal access changes recorded with actor; viewer in Settings (admin-only, last 100). Future: instrument creates/updates, invoicing, sign-ins; retention policy
- [ ] Data export (site promises "exports in full"; needs a real one-click export)
- [ ] Custom 404 exists; add error.tsx boundary pages
- [ ] "Add sample data" button in Settings (inverse of Remove — useful after the onboarding seed was skipped/failed, e.g. Paul's own workspace 2026-07-19)
- [ ] Telemetry ping implementation (currently a stub; needs Hub endpoint)
- [ ] Bitwarden import/export for the vault
- [ ] Portal logo branding upload (subdomain entry pages make this visible now)
- [x] Tenant subdomains *(2026-07-19)* — `acme.freeholdtc.dev` serves a branded client-portal entry page (middleware host routing; portal links display with the tenant subdomain; app paths redirect to apex; wildcard domain + cert on Vercel; works for self-hosters with their own wildcard DNS)
- [ ] Per-tenant custom portal domains (portal.acmerealty.com, CNAME + cert via Vercel API) — natural Business-tier feature; registrar domain-forwarding works today as the manual version
- [ ] Separate client-facing domain (e.g. freehold.estate) if Paul registers one — middleware already keys off one env var

## Stage 07 — Importers + integrations round 1 (in progress)

- [x] Import framework: CSV parser + header auto-mapping + dry-run preview *(2026-07-18)*
- [x] Generic CSV import for contacts and transactions (UI at /dashboard/import, sample CSV included) *(2026-07-18)*
- [ ] Vendor presets: header aliases cover the major legacy platforms' export shapes; still need verification against real export samples
- [ ] Email + calendar connections **without OAuth app reviews** (Paul's decision 2026-07-19 — no Google integration, ever): each TC connects their mailbox via **IMAP/SMTP** (works with any provider; Gmail needs an app password, which requires 2FA on their account — document this in the connect flow) with credentials stored in the existing vault; deadlines publish as a per-user **ICS calendar feed** (secret URL, read-only) that any calendar app subscribes to. Marketing updated to match (integrations page). Note: Microsoft 365 work accounts have basic-auth IMAP disabled by org policy sometimes — the connect flow should say so and fall back to SMTP-only or feed-only gracefully.
- [x] Public API v1 (API keys, transactions/contacts/tasks) + signed webhooks (transaction.created, task.completed) *(2026-07-18)*
- [x] API reference docs page at /docs/api (endpoints, auth, webhook signature verification example) *(2026-07-18)*
- [x] Webhook delivery retries with backoff (3 attempts, 4xx not retried, re-stamped signatures) *(2026-07-18)*
- [ ] Zapier app
- [ ] Twenty CRM two-way sync
- [x] Client invoicing: tenant invoices client via Stripe (hosted payment page, paid-status webhook) *(2026-07-18; live-verified with a paid test invoice)*
- [ ] Per-tenant Stripe Connect accounts for Cloud invoicing (v1 uses the configured platform/self-host key)

## Later stages (per PLAN.md)

- Stage 08: template library + marketplace (revenue share 70/30 proposed, confirm)
- Stage 09: comms round (BYO Twilio/Vonage SMS, reply-to-close, voice bridge)
- Stage 10: hardening + GA (backups to client-owned storage, retention, pen test, load test, RESO MLS adapter)
