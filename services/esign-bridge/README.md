# services/esign-bridge

API wrapper around a self-hosted [Documenso](https://documenso.com) instance for e-signature envelopes. Documenso itself runs as its own networked service, not a linked library — it's AGPL-3.0, and keeping it arm's-length keeps that copyleft out of this repo.
