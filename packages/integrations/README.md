# packages/integrations

Adapter interfaces (e-signature, email/SMTP, SMS, CRM, MLS) and their open-source adapter implementations — Documenso, DocuSign, Dotloop, Gmail/Outlook, Twilio, Vonage, Twenty CRM (via its API; Twenty is AGPL and its code is never embedded here). App code calls interfaces, never provider SDKs directly.
