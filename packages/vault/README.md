# packages/vault

Credential vault: envelope-encrypted per-client credentials (MLS, lender portals, e-sign accounts) with role-gated reveal and a full audit trail. Stores and audits only — no automated login with stored credentials. See [docs/PLAN.md](../../docs/PLAN.md#credential-vault).
